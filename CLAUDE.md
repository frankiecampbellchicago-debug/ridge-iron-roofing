# CLAUDE.md — Ridge & Iron Roofing

## Project rules for Claude Code

- Static HTML/CSS/JS only. No frameworks, no build tools.
- All file paths must be relative.
- Keep GitHub Pages and Netlify compatible.
- Do not remove or rename core sections.
- Color palette: charcoal (#141c28), copper (#b85c2a), off-white (#f4f2ee).
- Fonts: Barlow Condensed (display/headings), Barlow (body).
- CSS variables are defined in `:root` in styles.css — use them, don't hardcode colors.
- When editing index.html, never overwrite the entire file — make targeted changes only.
- The form is decorative (no backend). Do not add a backend or external service without asking.
- Keep accessibility: skip link, aria labels, focus styles, semantic HTML.
- Company name: Ridge & Iron Roofing LLC
- Phone: (708) 555-0192
- Address: 428 N. Rohlwing Road, Addison, IL 60101
